#!/bin/sh
# Every CI failure class this repository has actually had is caught by
# `make gate` in a couple of minutes. Bypass deliberately with --no-verify.
PY="${AGENT_ECONOMICS_PYTHON:-python3.12}"
command -v "$PY" >/dev/null 2>&1 || PY=python3
exec make PYTHON="$PY" gate
